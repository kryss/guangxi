<!DOCTYPE html>
<html>
<head>
  <title>GuangXi</title>
  <%= stylesheet_link_tag "web-app-theme/base", "web-app-theme/themes/warehouse/style", "web-app-theme/override", :cache => true %>
  <%= javascript_include_tag :defaults, :cache => true %>
  <%= csrf_meta_tag %>
</head>
<body>
  <div id="container">
    <div id="header">
      <h1><%= link_to t(:title), root_path  %></h1>
      <div id="user-navigation">
        <ul class="wat-cf">
          <li><a href="#"><%= t("link.show_profile", :default => "Profile") %></a></li>
           <li><%= link_to t("link.edit_profile", :default => "Edit Profile"), edit_user_registration_path %></li>
          <li><%= link_to t("devise.logout", :default => "Logout"), destroy_user_session_path, :method => :delete %></li>
        </ul>
      </div>
      <div id="main-navigation">
        <ul class="wat-cf">
        	<li><%= link_to t(:title), root_path  %></li>
			<li><%= link_to t(:title), root_path  %></li>
			<li><%= link_to t(:title), root_path  %></li>
        </ul>
      </div>
    </div>    
    <div id="wrapper" class="wat-cf">
      <div class="flash">
        <% flash.each do |type, message| -%>
          <div class="message <%= type %>">
            <p><%= message %></p>
          </div>
        <% end -%>
      </div>
      <div id="main" style="width:<%= (content_for?(:sidebar)) ? (73) : (100) %>%">
        <%= yield %>
        <div id="footer">
          <div class="block">
            <p>Copyright &copy; <%= Time.now.year %> GuangXi.</p>
          </div>      
        </div>
      </div>
      <div id="sidebar">
        <%= yield :sidebar %>
      </div>
    </div>    
  </div>
<%= debug(params) if Rails.env.development? %>
</body>
</html>
